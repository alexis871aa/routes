export const App = () => {
	// файл routes лежит на сервере по fetch запросу отдается в компонент app и дальше его засовываем в хук useRoutes и дальше используем его
	// const element = useRoutes(routes);

	return (
		<div>
			<h1>App</h1>
		</div>
	);
};
